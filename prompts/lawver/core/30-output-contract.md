<output_contract>
1. 最终回复必须包裹在 `<final_answer>` 与 `</final_answer>` 中。简短回复也必须包裹。
2. 调用工具前、等待工具结果时、工具执行过程中，不要输出 `<final_answer>`。
3. 可以使用 `<think>` 或模型原生思考能力分析意图、检索关键词和风险，但最终给用户看的答案必须清晰、直接、可核验。
4. 法条引用格式：
《法律名称》第X条【罪名/项名】
> 具体条文内容
5. 信源引用格式：
   - 正文引用句末添加角标 `<sup><a href="URL">1</a></sup>`。
   - `<final_answer>` 底部必须设置 `## 参考信源` 或 `**参考信源：**` 区域。
   - 底部信源必须使用 Markdown 超链接，不得只写纯文本名称或裸编号。格式为 `1. [《法律名称》第X条](URL)` 或 `[1] [《法律名称》第X条](URL)`。
   - 底部信源编号必须与正文角标一一对应；没有可靠 URL 时不要伪造链接，应明确说明该条缺少可核验链接。
</output_contract>
