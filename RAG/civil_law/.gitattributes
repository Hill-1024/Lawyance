# 语料按字节原样存取，禁止 git 做换行符归一化。
#
# 索引指纹是 sha256(每个文件的字节)，仓库开了 core.autocrlf=true 时，CRLF 语料在索引里
# 会被存成 LF、检出时再还原——Windows 上能往返，但 Linux 服务器克隆出来就是 LF，
# 与开发机字节不同，指纹随之不同，部署后第一次启动会白白多做一次整库重建。
# 标成 -text 后，语料在所有平台都是逐字节一致的。
data/** -text

# 代码与文档仍走常规文本处理，便于 diff。
*.py text
*.md text
